# Commit Coach brand assets

- `icon.svg` — square icon with progress ring and checkmark.
- `logo-horizontal.svg` — horizontal logo lockup.
- `logo-stacked.svg` — stacked logo lockup.

## Export PNGs

```sh
npm run export:brand
```

This script generates:
- `icon-512.png`
- `icon-1024.png`
